<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/profile_photos_root"
    android:layout_width="match_parent"
    android:layout_height="1320px"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/profile_photos_dialog_image"
        android:layout_width="match_parent"
        android:layout_height="1320px"
        android:src="@drawable/upcoming_event" />

    <TextView
        android:id="@+id/profile_photos_dialog_close"
        android:layout_width="match_parent"
        android:layout_height="140px"
        android:layout_alignParentBottom="true"
        android:background="#6e00ccd2"
        android:gravity="center_vertical"
        android:text="Close"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:textSize="70px" />

</RelativeLayout>